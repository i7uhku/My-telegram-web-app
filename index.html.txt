<!DOCTYPE html>
<html>
<head>
    <title>Моя первая программа</title>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-family: Arial;
            text-align: center;
            padding: 50px;
        }
        .btn {
            background: white;
            color: #667eea;
            padding: 15px 30px;
            border-radius: 25px;
            font-size: 20px;
            border: none;
            margin: 20px;
            cursor: pointer;
        }
        .btn:hover {
            background: #f0f0f0;
        }
    </style>
</head>
<body>
    <h1>🎉 Моё первое приложение! 🎉</h1>
    <p>Я сделал это сам!</p>
    
    <button class="btn" onclick="sayHello()">Нажми меня!</button>
    <button class="btn" onclick="changeColor()">Сменить цвет</button>
    
    <div id="counter">
        <p>Счётчик: <span id="count">0</span></p>
        <button class="btn" onclick="add()">+1</button>
    </div>
    
    <script>
        // Простые функции
        function sayHello() {
            alert('Привет! Я работаю! 🚀');
        }
        
        function changeColor() {
            const colors = ['#FF6B6B', '#4ECDC4', '#FFD166', '#06D6A0', '#118AB2'];
            const randomColor = colors[Math.floor(Math.random() * colors.length)];
            document.body.style.background = randomColor;
        }
        
        let count = 0;
        function add() {
            count++;
            document.getElementById('count').textContent = count;
        }
        
        // Проверяем, открыто ли в телеграме
        if (window.Telegram && window.Telegram.WebApp) {
            document.body.innerHTML += '<p>📱 Открыто в Telegram!</p>';
        }
    </script>
</body>
</html>